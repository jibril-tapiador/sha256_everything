# frozen_string_literal: true

RSpec.describe Sha256Everything do
  it "has a version number" do
    expect(Sha256Everything::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
